# groguv12 ZMK Configuration

# USB only (no BLE for now)
CONFIG_ZMK_USB=y
CONFIG_ZMK_BLE=n

# SPI for shift registers
CONFIG_SPI=y

# Custom kscan driver
CONFIG_KSCAN_SR_MATRIX=y

# Logging (disable for production for better performance)
CONFIG_ZMK_LOG_LEVEL_DBG=n
